#!/bin/sh
repo="$(git rev-parse --show-toplevel)" || exit 0
if command -v pwsh >/dev/null 2>&1; then
  pwsh -NoProfile -File "$repo/.agents/skills/project-coordination/scripts/coord.ps1" hook-post-commit >/dev/null 2>&1 || true
fi
exit 0

